name: Build

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    name: make all
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: 'go.mod'
      - name: make build
        run: make all
